
from setuptools import setup, find_packages

setup(
    name="my_github_project",
    version="0.1.0",
    description="A template GitHub project",
    author="Afridi Jubair",
    packages=find_packages(),
    install_requires=[
        "requests"
    ],
)
